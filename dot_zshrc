# shell cmd customization
export PS1="🎮 /%2~: "

[ "$TERM" = "xterm-kitty" ] && alias ssh="kitty +kitten ssh"

eval "$(/usr/local/bin/brew shellenv)"
eval "$(/Users/jish/.local/bin/mise activate zsh)"
eval "$(starship init zsh)"

